using UnityEngine;

namespace RS.Scene
{
    public class Map
    {
        private Vector2 m_startChunkPos;
        private int m_size;
        private Texture2D m_map;

        // public void InitMap()
        // {
        //     var mapData = ChunkManager.Instance.GetMapData(m_startChunkPos, m_size);
        //     m_map = new Texture2D(m_size, m_size);
        //
        //     
        // }
    }
}